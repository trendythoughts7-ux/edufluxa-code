/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
/*!*************************************************************!*\
  !*** ./resources/js/admin/parts/landing_builder_welcome.js ***!
  \*************************************************************/
(function () {
  "use strict";

  $(document).ready(function () {
    var $progress = $('.js-progress-card');
    if ($progress.length) {
      var url = $progress.attr('data-url');
      var seconds = parseInt($progress.attr('data-seconds')) || 30;
      var $bar = $progress.find('.progress-bar');
      $({
        width: 0
      }).animate({
        width: 100
      }, {
        duration: seconds * 1000,
        easing: 'linear',
        step: function step(now) {
          $bar.css('width', "".concat(now, "%"));
        },
        complete: function complete() {
          if (url) {
            window.location.href = url;
          }
        }
      });
    }
  });
})(jQuery);
/******/ })()
;