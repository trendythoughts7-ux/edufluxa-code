/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
/*!****************************************************!*\
  !*** ./resources/js/design_1/panel/noticeboard.js ***!
  \****************************************************/
(function ($) {
  "use strict";

  $(document).ready(function () {
    if (jQuery().summernote) {
      makeSummernote($('.summernote'));
    }
  });
  $('body').on('click', '.js-view-message', function (e) {
    var $this = $(this);
    var card = $this.closest('.noticeboard-item');
    var title = card.find('.js-noticeboard-title').text();
    var time = card.find('.js-noticeboard-time').text();
    var message = card.find('.js-noticeboard-message').val();
    var modal = $('#noticeboardMessageModal');
    modal.find('.modal-title').text(title);
    modal.find('.modal-time').text(time);
    modal.find('.modal-message').html(message);
    Swal.fire({
      html: modal.html(),
      showCancelButton: false,
      showConfirmButton: false,
      customClass: {
        content: 'p-0 text-left'
      },
      width: '30rem'
    });
  });
  $('body').on('click', '.js-submit-noticeboard-form', function (e) {
    e.preventDefault();
    var $this = $(this);
    var $form = $this.closest('form');
    var action = $form.attr('action');
    handleSendRequestItemForm($form, $this, action);
  });
  $('body').on('change', '.js-noticeboard-type-select', function (e) {
    e.preventDefault();
    var value = $(this).val();
    var instructorCourses = $('.js-instructor-courses-fields');
    instructorCourses.addClass('d-none');
    if (value === 'course') {
      instructorCourses.removeClass('d-none');
    }
  });
})(jQuery);
/******/ })()
;