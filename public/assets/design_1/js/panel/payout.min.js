/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
/*!***********************************************!*\
  !*** ./resources/js/design_1/panel/payout.js ***!
  \***********************************************/
(function ($) {
  "use strict";

  $('body').on('click', '.js-request-payout-modal', function (e) {
    e.preventDefault();
    var html = $('#requestPayoutModal').html();
    var footer = "<div class=\"d-flex align-items-center justify-content-end\">\n            <form method=\"post\" action=\"/panel/financial/payout/request\">\n                <input type=\"hidden\" name=\"_token\" value=\"".concat(window.csrfToken, "\">\n                <button type=\"button\" class=\"js-submit-payout btn btn-primary\">").concat(submitRequestLang, "</button>\n            </form>\n        </div>");
    var body = makeModalHtml(payoutRequestLang, closeIcon, html, footer);
    Swal.fire({
      html: body,
      showCancelButton: false,
      showConfirmButton: false,
      width: '36rem',
      didOpen: function didOpen() {}
    });
  });
  $('body').on('click', '.js-submit-payout', function (e) {
    e.preventDefault();
    $(this).addClass('loadingbar primary').prop('disabled', true);
    $(this).closest('form').trigger('submit');
  });
  $('body').on('click', '.js-show-details', function () {
    var $this = $(this);
    var path = $this.attr('data-path');
    handleBasicModal(path, payoutDetailsLang, function (result, $body, $footer) {
      $footer.remove();
    }, '', '36rem');
  });
})(jQuery);
/******/ })()
;