/******/ (() => { // webpackBootstrap
var __webpack_exports__ = {};
/*!**********************************************************!*\
  !*** ./resources/js/design_1/parts/instructors_lists.js ***!
  \**********************************************************/
(function ($) {
  "use strict";

  function handleSelectedCategoriesHtml(id, text) {
    return "<div class=\"js-selected-filter-parent d-inline-flex-center bg-gray-100 p-8 rounded-8 text-gray-500\">\n                    <input type=\"hidden\" name=\"skills[]\" value=\"".concat(id, "\">\n                    <div class=\"js-remove-selected-filter d-flex-center size-16 rounded-4 bg-gray-400 cursor-pointer\">").concat(selectedCloseIcon, "</div>\n                    <span class=\"ml-8\">").concat(text, "</span>\n                </div>");
  }
  function checkCanSelectCategory(id) {
    var check = $("input[value=\"".concat(id, "\"]"));
    return check.length < 1;
  }
  $('body').on('change', '.js-skills-select', function (e) {
    var $this = $(this);
    var $selectedOption = $this.find('option:selected');
    var $selectedCategories = $('.js-selected-category-filters');
    var text = $selectedOption.text();
    var id = $selectedOption.attr("value");
    if ($selectedCategories.length && text && id) {
      if (checkCanSelectCategory(id)) {
        $selectedCategories.append(handleSelectedCategoriesHtml(id, text));
      }
      $this.val("");
      $this.trigger("change");
    }
  });
  $('body').on('click', '.js-remove-selected-filter', function (e) {
    e.preventDefault();
    $(this).closest('.js-selected-filter-parent').remove();
  });
})(jQuery);
/******/ })()
;